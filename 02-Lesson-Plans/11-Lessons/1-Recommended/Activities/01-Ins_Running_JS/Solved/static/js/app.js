
// This code will run when linked to in HTML
console.log("My script is stored outside of the HTML!");

// Print the data if data.js is linked to in HTML
console.log(data);

// Print the first data value if data.js is linked to in HTML
console.log(data[0]);
